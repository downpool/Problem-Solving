//배열 두배 만들기
#include <bits/stdc++.h>

using namespace std;

vector<int> solution(vector<int> numbers) {
    vector<int> answer;
    for (int a : numbers) {
        answer.push_back(a * 2);
    }
    return answer;
}

//두수의 나눗셈
#include <bits/stdc++.h>

using namespace std;

int