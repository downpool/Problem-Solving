#include <bits/stdc++.h>
using namespace std;
#define INF 1000000000;

int dop[5001];
int size;

int dp(int a) {
    int temp5 = a - 5;
    int temp3 = a - 3;
    if (temp5 % 3 == 0 || temp5 % 5 == 0) {
    }
}

void solve() {
    cin >> size;
    memset(dp, 5000, 5000);
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    solve();
}