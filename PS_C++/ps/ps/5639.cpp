#include <bits/stdc++.h>
using namespace std;
#define INF 1000000000;

void solve() {
    vector<int> tree;
    int n;

    while (cin >> n) {
        tree.push_back(n);
    }
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    solve();
}