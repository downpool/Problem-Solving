let fs = require('fs');
let input = fs.readFileSync('/dev/stdin').toString().trim().split('\n');
//입력 형태 변환
let fir = input[0];

function solution(n){
    let answer = 0;
    
    function DP(l, )

    return answer;
}
console.log(solution(fir));